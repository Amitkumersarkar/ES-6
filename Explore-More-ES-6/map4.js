const friends = ['amit', 'amrito', 'myself'];
const lengths = friends.map(friend => friend.length);
// console.log(lengths);

// if needed to find out 1st index number 

const firstLetter = friends.map(friend2 => friend2[0]);
console.log(firstLetter);